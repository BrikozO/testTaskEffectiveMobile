services:
  postgres_db:
    image: postgres:latest
    volumes:
      - postgresql_data:/var/lib/postgresql/data
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"

volumes:
  postgresql_data: